# Generated by Quality Graph. DO NOT EDIT MANUALLY. Update qg.yaml and run qg generate.
---
name: Quality Graph
'on':
  pull_request:
    branches:
      - main
  workflow_dispatch: {}
permissions:
  contents: read
concurrency:
  group: quality-graph-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  workflow-graph:
    name: Workflow graph validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: astral-sh/setup-uv@v10.0.1
      - run: uvx --from quality-graph-cli==0.1.8 --with quality-graph-github==0.1.8 qg validate
        name: Run Workflow graph validation
        id: quality-command
        continue-on-error: true
      - name: Collect Workflow graph validation result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: workflow-graph
          title: Workflow graph validation
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: 8619f3661d4bbf1caf3ab9984ce80f0a4f8ba074115da6d2405d965d23359ef0
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Workflow graph validation result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-workflow-graph-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/workflow-graph.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Workflow graph validation result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - run: make build
        name: Run Build
        id: quality-command
        continue-on-error: true
      - name: Collect Build result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: build
          title: Build
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: 8619f3661d4bbf1caf3ab9984ce80f0a4f8ba074115da6d2405d965d23359ef0
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Build result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-build-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/build.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Build result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make test-cov
        name: Run Coverage
        id: quality-command
        continue-on-error: true
      - name: Collect Coverage result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: coverage
          title: Coverage
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: 8619f3661d4bbf1caf3ab9984ce80f0a4f8ba074115da6d2405d965d23359ef0
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Coverage result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-coverage-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/coverage.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Coverage result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  golangci-lint:
    name: GolangCI-Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make golangci-lint
        name: Run GolangCI-Lint
        id: quality-command
        continue-on-error: true
      - name: Collect GolangCI-Lint result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: golangci-lint
          title: GolangCI-Lint
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: 8619f3661d4bbf1caf3ab9984ce80f0a4f8ba074115da6d2405d965d23359ef0
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload GolangCI-Lint result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-golangci-lint-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/golangci-lint.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce GolangCI-Lint result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make test
        name: Run Test
        id: quality-command
        continue-on-error: true
      - name: Collect Test result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: test
          title: Test
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: 8619f3661d4bbf1caf3ab9984ce80f0a4f8ba074115da6d2405d965d23359ef0
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Test result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-test-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/test.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Test result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  integration:
    name: Integration tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make integration-test
        name: Run Integration tests
        id: quality-command
        continue-on-error: true
      - name: Collect Integration tests result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: integration
          title: Integration tests
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: 8619f3661d4bbf1caf3ab9984ce80f0a4f8ba074115da6d2405d965d23359ef0
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Integration tests result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-integration-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/integration.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Integration tests result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
