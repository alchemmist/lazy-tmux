# Generated by Quality Graph. DO NOT EDIT MANUALLY. Update qg.yaml and run qg generate.
---
name: Quality Graph (push)
'on':
  push:
    branches:
      - main
permissions:
  contents: read
concurrency:
  group: quality-graph-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  workflow-graph:
    name: Workflow graph validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: astral-sh/setup-uv@v10.0.1
      - run: uvx --from quality-graph-cli==0.1.8 --with quality-graph-github==0.1.8 qg validate
        name: Run Workflow graph validation
        id: quality-command
        continue-on-error: true
      - name: Collect Workflow graph validation result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: workflow-graph
          title: Workflow graph validation
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Workflow graph validation result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-workflow-graph-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/workflow-graph.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Workflow graph validation result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - run: make build
        name: Run Build
        id: quality-command
        continue-on-error: true
      - name: Collect Build result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: build
          title: Build
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Build result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-build-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/build.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Build result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make test-cov
        name: Run Coverage
        id: quality-command
        continue-on-error: true
      - name: Collect Coverage result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: coverage
          title: Coverage
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Coverage result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-coverage-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/coverage.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Coverage result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  golangci-lint:
    name: GolangCI-Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make golangci-lint
        name: Run GolangCI-Lint
        id: quality-command
        continue-on-error: true
      - name: Collect GolangCI-Lint result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: golangci-lint
          title: GolangCI-Lint
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload GolangCI-Lint result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-golangci-lint-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/golangci-lint.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce GolangCI-Lint result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make test
        name: Run Test
        id: quality-command
        continue-on-error: true
      - name: Collect Test result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: test
          title: Test
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Test result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-test-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/test.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Test result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  integration:
    name: Integration tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: 'false'
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Setup tools
        run: make setup-env
      - run: make integration-test
        name: Run Integration tests
        id: quality-command
        continue-on-error: true
      - name: Collect Integration tests result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: integration
          title: Integration tests
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Integration tests result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-integration-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/integration.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Integration tests result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - workflow-graph
  tmux-versions:
    name: Tmux version test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: 'false'
          fetch-depth: '0'
      - run: ./scripts/quality-graph-tmux-versions.sh
        name: Run Tmux version test
        id: quality-command
        continue-on-error: true
      - name: Collect Tmux version test result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@b947f26e97ccf1c755050dfe38d98cbb688edb69
        with:
          operation: collect
          node-id: tmux-versions
          title: Tmux version test
          adapter: native
          report-path: .cache/quality-graph/tmux-versions.json
          command-outcome: ${{ steps.quality-command.outcome }}
          graph-digest: de7217a2debee71075d2b94d82e0e8e65188deb58b4c34636761be77796108ce
          approval-findings: 'true'
          approval-files: 'false'
          approval-node: 'false'
      - name: Upload Tmux version test result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: quality-result-tmux-versions-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/tmux-versions.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Tmux version test result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    timeout-minutes: 60
